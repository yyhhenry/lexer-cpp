Var 
    i, j1: integer; SUM: longint;
    Flag: bool;

begin
    i := 0; 
    j1 := 50;
    sum := 1;
    flag := 1;
    while (i < j1) and (flag) do
    BEGIN
        i := i + 1;
        sum := sum * i;
        if (sum > 65535 / (i + 1)) then
            flag := 0;
    END;
end
